#!/bin/zsh
# source ~/memes.log
# LOGO=$(basename ${system_background})
# neofetch --kitty ~/Pictures/icons/logos/${LOGO%%.*}.png
# neofetch
fastfetch
LC_TIME="ko_KR.utf8" date "+ %A-%B%d일"
exec zsh
